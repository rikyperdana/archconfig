# Loc: ~/.config/tmux/tmux.conf

# Change default binder
unbind C-b
set-option -g prefix `
bind ` send-prefix

# Start window numbering at 1
set -g base-index 1
setw -g pane-base-index 1

# Panes control
set -g display-panes-time 4000